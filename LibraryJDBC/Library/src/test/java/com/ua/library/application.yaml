spring:
  datasource:
    url: jdbc:h2:mem:springCamp
  sql:
    init:
      mode: always
      data-locations: data.sql
      schema-locations: schema.sql
  shell:
    interactive:
      enable: false